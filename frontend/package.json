{
  "name": "patchticker-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.mjs",
    "audit:check": "npm audit --audit-level=moderate"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "7.3.6"
  }
}
